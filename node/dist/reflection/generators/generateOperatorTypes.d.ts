import type { GeneratorParams } from "../generate";
export declare function generateOperatorFunctions({ dir, operators, types, casts, isDeno, }: GeneratorParams): void;
export declare function generateOperators({ dir, operators, types, casts, isDeno, }: GeneratorParams): void;
